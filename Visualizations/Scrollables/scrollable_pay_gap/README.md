# Future scrollable visualization

This directory is reserved for the second scrollytelling visualization. Reuse the Vite scripts and conventions established by `../scrollable_map/`; add its own page and locally prepared data only when its story and source data are defined.
